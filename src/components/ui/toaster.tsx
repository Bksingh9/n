'use client';

// Minimal "toaster" placeholder. We render flash messages via search params
// or inline alerts in this MVP; this stub keeps the layout slot stable so we
// can drop in a real toast provider without a layout change.

export function Toaster() {
  return null;
}
